import {Colors} from '../../../styles';

const styles = {
  container: {
    flex: 1,
  },
  fab: {
    position: 'absolute',
    margin: 12,
    right: 8,
    bottom: 20,
    backgroundColor: Colors.PRIMARY,
  },
  containerSearch: {
    backgroundColor: Colors.PRIMARY,
    justifyContent: 'center',
    alignItems: 'center',
    paddingVertical: 12,
  },
};

export default styles;
